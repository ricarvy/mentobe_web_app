# ========================================
# Supabase 环境变量配置
# ========================================
# 将此文件复制为 .env.local 并填入你的 Supabase 配置

# Supabase 项目 URL
# 在 Supabase Dashboard: Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase 匿名密钥（客户端使用）
# 在 Supabase Dashboard: Settings → API → anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase 服务角色密钥（服务端使用）
# 在 Supabase Dashboard: Settings → API → service_role key
# ⚠️ 此密钥具有完全访问权限，请勿暴露给客户端
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 数据库连接字符串（用于 Drizzle ORM）
# 在 Supabase Dashboard: Settings → Database → Connection String → URI
# 格式: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.your-project-ref.supabase.co:5432/postgres

# 数据库连接池配置（可选）
DB_POOL_MIN=2
DB_POOL_MAX=10

# 应用配置（保持现有配置）
NEXT_PUBLIC_APP_URL=http://localhost:5000

# ========================================
# 获取步骤：
# ========================================
# 1. 登录 https://supabase.com
# 2. 选择你的项目
# 3. 进入 Settings → API
# 4. 复制以下信息：
#    - Project URL
#    - anon/public key
#    - service_role key
# 5. 进入 Settings → Database
# 6. 找到 Connection string (URI)
# 7. 替换 [YOUR_PASSWORD] 为你的数据库密码
#    （在 Database settings 中可以查看或重置）
